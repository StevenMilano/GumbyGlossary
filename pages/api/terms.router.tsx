import { NextApiRequest, NextApiResponse } from 'next';
import { ObjectId } from "mongodb";

export default async function termsRouter(req: NextApiRequest, res: NextApiResponse) {
    //finish connecting to db
}